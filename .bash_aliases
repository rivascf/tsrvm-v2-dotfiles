# General aliases
alias update-upgrade='sudo apt-get -y update && sudo apt -y update && sudo apt -y upgrade'
alias update-all='sudo apt-get -y update && sudo apt -y update'
alias reset-term='source ~/.bashrc'

# Git aliases
alias   g='git'
alias gst='g status -sb'

# Change default vim for neovim
alias vim='nvim'


